# Railway works without this file — it's here only to pin the start command
# explicitly if Railway ever fails to auto-detect Next.js.
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
